import React, { Component } from "react";import {AsyncStorage, Image, Text} from 'react-native';import {connect} from "react-redux";import {chooseLang, profile, userLogin , logout, tempAuth} from "../actions";import styles from "../../assets/style";import i18n from "../../locale/i18n";class DetermineLocation extends Component {	static navigationOptions = () => ({		header          : null,		drawerLabel     : (<Text style={[styles.textRegular, styles.textSize_16]}>{i18n.translate('determineLocation')}</Text>) ,		drawerIcon      : (<Image style={[styles.headImage]} source={require('../../assets/img/locations.png')} resizeMode={'contain'}/>)	});	render() {		return false;	}}const mapStateToProps = ({ auth, profile, lang }) => {	return {		auth: auth.user,		user: profile.user,		lang: lang.lang	};};export default connect(mapStateToProps, {})(DetermineLocation);